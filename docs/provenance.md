# Provenance and Project

## Changed files

App files below are under `Remember/Remember/`.

- Added: `Provenance.swift`, `ProjectGraphService.swift`, `ProjectIntelligence.swift`, `ProjectViewModel.swift`, `ProvenanceViews.swift`.
- Updated persistence/capture: `MemoryItem.swift`, `MemoryStore.swift`, `MemoryPipeline.swift`, `LibraryViewModel.swift`.
- Updated interface/privacy: `ContentView.swift`, `ProjectView.swift`, `MemoryDetailView.swift`, `SettingsView.swift`, `PrivacyActivity.swift`, `PrivacyDashboardView.swift`.
- Tests: added `Remember/RememberTests/ProvenanceTests.swift`, `ProjectClusteringTests.swift`, `RecordedProjectEmbeddingTests.swift`, and the synthetic `project-embedding-device-v5.json` fixture; updated `Remember/RememberUITests/RememberUITests.swift`. A separate reusable physical-device probe lives under `scripts/embedding-evaluation/`.
- Documentation: updated `readme.md`; added `docs/provenance.md`. The supplied feature spec was not modified.

## Data and recovery

`provenanceEvent` is an append-only GRDB/SQLite ledger with a monotonically increasing sequence, unique event ID, recorded timestamp, source ID, origin, and versioned JSON payload. SQLite triggers reject UPDATE and DELETE. Capture payloads preserve original capture timestamps separately from the event’s recorded timestamp. Capture events include OS and time-zone context without a device identifier.

The `memory` table remains the mutable current-state projection used by Memories and retrieval. Memory changes and their events commit in the same database transaction. Notes edited through the app receive a new protected payload file; old file references remain valid. Restore reuses an immutable previous payload and appends a revision. Files are staged before database references; interrupted captures are retried by stable capture ID. A crash between staging a note revision and recording it can leave an unreferenced file, which is retained rather than risking removal of history.

The additive `createImmutableProvenanceV1` migration backfills existing memories once. These are labelled imported history, recorded at upgrade time; the app cannot reconstruct revisions or classifications overwritten by earlier versions. Legacy Wiki tables remain dormant. Do not delete ledger rows to roll back a correction: use a compensating event. Downgrading to an older app build that predates the ledger is unsupported because it can bypass provenance recording.

Archive replaces memory deletion throughout the app. Originals, extracted content, and events are retained; archived memories are excluded from normal library, collection counts, search, and Ask retrieval. The archive in Project and Settings supports restoration. There is no permanent-erasure action in this version. Storage therefore grows with capture and revision history.

Thread deletion is separate from memory archival: an `archive`/`restore` event with a nil memory ID and a payload cluster ID hides/restores only that thread. No memory records, original files, or shared memberships are removed. Archived thread memberships are protected from automatic placement and merging without adding permanent user pins. Restoration releases that protection. Settings → Archive lists both kinds of recoverable items. This uses the existing event schema; older builds will ignore thread archival and show those threads again, so downgrading does not preserve their hidden state.

## Current local organization — D3 integration, 14 September 2026

`ProjectViewModel` now instantiates `D3ProjectOrganizer`, not `ProjectGraphService`. The bundled P2 seed-29 MiniLM matcher combines with Apple contextual/sentence embeddings and the frozen lexical feature classifier. C3 corroboration places a new capture only when exactly one thread has sufficient supporting members. The policy is `d3-p2-seed29-corroborated-v1`; its implementation, parity evidence, boundaries and reproducible commands are in [D3 organization](d3-organization.md).

This is an explicitly requested experimental integration, not a new qualification result. The P2 all-seeds gate remains failed. Existing placements are not migrated or automatically regrouped. New decisions append source-bound evidence to the same ledger and use sequence checks; user assignments, renames, archived threads and blocked pairs remain protected. D3 does not automatically merge threads, generate batch split proposals, or invoke a local/cloud generative fallback. Historical undo, split acceptance and past-state navigation remain available.

Cloud assistance now affects capture enrichment only; Ask and AI search remain separate explicit cloud actions. D3 itself never transmits source content. English text up to 16,000 Unicode scalars is eligible; unavailable models defer matching without losing the capture. This text-only scope does not add image understanding or video/audio analysis beyond the existing extraction pipeline.

For the next product tier, see [Remember Pro cloud review](premium-organization-api.md):
the separate GPT-5.4 pilot showed promising context judgments, but paid-user grouping,
consent and entitlement enforcement are not implemented. All current D3 provenance
writes remain local. The [iOS 27 assessment](ios27-free-tier-assessment.md) records
new local-model opportunities without changing the ledger, thresholds or placements.

## Previous local organizer — historical reference, not the live default

Capture creates a singleton without waiting for models. Local extraction is recorded before generated metadata. Apple Vision, PDFKit/text extraction, and on-device speech produce text; Foundation Models supplies metadata when available. Unavailable or failed generation retains deterministic source-derived metadata.

Project organization keeps two independent local similarity signals: contextual token averages over bounded slices and length-weighted normalized sentence embeddings over sentence-aware, at-most-256-character chunks. Neither score can substitute for the other. The stored space identifies the language, both model revisions/dimensions, contextual model ID, and both pooling strategies. Incompatible spaces are never compared. `semanticVector` is an optional additive JSON payload field, so older ledger events remain readable without a database migration. Empty or unsupported text and missing models retain singleton placement with an explanation. Contextual asset availability triggers a retry, and foregrounding retries pending work; repeated asset requests are throttled.

Automatic decisions also require independent source grounding: at least two shared lemmatized content terms for every affected source pair, excluding generic capture/style words. Identical normalized, meaningful source text is also eligible. This prevents similarly written but unrelated notes from joining solely because their embeddings are close. Source-term evidence is cached by memory ID and exact source text, so edits invalidate it.

`ProjectGraphService` records placement vectors and rationale with their source enrichment/revision event. Sequence checks reject asynchronous decisions computed against older organization. User assignments are pinned and remain intact during automatic placement and merging. Renames survive re-enrichment. Metadata corrections survive generated metadata refreshes until a new content revision.

The previous policy was `grounded-dual-v5-place094-margin001-member092-merge095-sem040-cross030-terms2`. Direct placement required contextual centroid cosine ≥0.94, a ≥0.01 lead among source-grounded candidates, sentence centroid cosine ≥0.40, and support from every member (contextual ≥0.92 and sentence ≥0.30). A candidate cluster had to be coherent. Bounded reasoning over at most two candidates could handle uncertain matches, including paraphrases without literal term overlap, but member-level vector guards still applied. The following batch/merge behavior describes that retained reference implementation, not D3.

Batch review runs after three placements in the first week, twenty thereafter, or on a foreground pass when the last checkpoint is over a day old. Completed event-level work survives cancellation and relaunch. This is foreground/opportunistic work, not a guaranteed nightly background task.

Automatic merging requires contextual centroid similarity ≥0.95, sentence centroid similarity ≥0.40, coherent existing clusters, the source-term gate, and member-level contextual/sentence support for **every cross-cluster pair**, not just centroids. Pinned assignments and rejected pairs prevent automatic merging. Splits of incoherent clusters with at least four sources are proposed when a coherent subgroup can be separated; acceptance checks memberships have not changed. Undo restores affected memberships only when still applicable and preserves unrelated later events. Undoing a merge records a blocked pair and pins affected memberships to prevent immediate recurrence.

The [initial iPhone 17 evaluation](evaluations/2026-09-07-iphone17-embeddings.md) exposed incorrect mixed-topic clustering and capture-order sensitivity in the original policy. The [grounded-policy follow-up](evaluations/2026-09-07-grounded-clustering.md) documents the fix and broader regression results. These remain conservative empirical rules, not probability-calibrated confidence estimates or universal language guarantees. Future policies must use new version identifiers without rewriting past decisions.

Policy/model upgrades regenerate evidence through new placement events. Old-policy vectors are excluded even while an upgrade is only partly complete, and a later unavailable vector invalidates earlier cached evidence. Existing memberships and user pins are preserved; an old mixed cluster can receive a split suggestion but is not silently dismantled. Review and accept such suggestions to correct earlier organization.

## Interface and privacy

The map's layout and focus state are display-only. Hexagonal rings place the largest thread at the center, preserving source indices and IDs. Circle centers stay locked to a rigid honeycomb: each is its grid coordinate plus one shared pan translation, with no radial position warping or focus-driven neighbor displacement. A size-only radial lens scales diameters by `1.16 / sqrt(1 + (r/R)²)`, so circles grow toward the viewport center without bending the rows. Lens radius adapts to the viewport, and baseline size remains independent of library size. The selected circle lifts another 6% within its own slot; lattice spacing reserves room for maximum magnification and focus, so no circle needs to push another aside. Related circles/edges use the existing shared-source/tag relationships; unrelated circles soften without disappearing. Connections interpolate with circle movement and end at their projected rims. Satin graphite standard materials in Dark Mode and soft neutral surfaces in Light Mode share a subtle cool-silver tint. A faint reflection and a single restrained rim respond to viewport position; center prominence and focus adjust brightness without assigning arbitrary colors to threads. No decorative connections, media icons, numeric node badges, or idle animation are added.

A hold opens a compact material preview on the map with the full title, memory count, actual related-thread count, and up to three sorted tags. Its button opens the River. A quick tap still opens the River directly with SwiftUI's [native zoom navigation transition](https://developer.apple.com/documentation/swiftui/zoomnavigationtransition), without an intermediate preview screen. Opening a River clears the preview. Focus/preview can also be cleared from the map background, Clear focus, Recenter, or leaving the active scene. VoiceOver has an explicit Preview thread action in addition to Highlight connections.

Circle labels are display-only aliases of up to three whole words, not saved thread renames. Longer titles omit a small set of English connective/introductory words and retain the first two remaining words plus the last qualifier; for example, “Digital Entrepreneurship Class Important Dates” becomes “Digital Entrepreneurship Dates.” Short titles are preserved. Words are never cut at a character boundary or given an added ellipsis. The label uses up to three lines with one measured font size for the entire block, fitted inside the circle. Exceptional unbroken tokens that would need a base font below 11 points use “Open thread” instead of microscopic text. No model or network requests are made; these are predictable display heuristics, not semantic summaries.

The map header shows only its heading and counts; no separate centered-node caption is displayed. Original full titles remain available in the press/hold preview, inline thread search, VoiceOver labels and thread history. Find a thread uses native search on the Threads page: focus shows active-thread suggestions, typing filters them locally, and closing search returns to the map.

Dragging follows the finger continuously; on release the nearest occupied circle snaps into the fixed lens center with a 0.32-second smooth animation. There is no velocity-based skipping or free-scrolling resting position. Small drags return to the same circle; ties resolve in stable source order. The settled circle receives focus without automatically opening its preview or River. Accessibility scrolls and viewport/layout changes use the same nearest-node settling rule; Reduce Motion settles immediately. Recenter still returns to the initial central thread.

`ProjectGraphMotion` owns a single presentation offset for both dragging and settling. The gesture no longer combines a resetting translation with a separately committed destination. A display link advances a deterministic smoothstep curve over elapsed time; position and size are recalculated together from that frame's offset, without a second implicit position animation. A new drag cancels settling and starts from the currently visible position. Gesture cancellation also settles; disappearance/backgrounding stops the display link and leaves an occupied slot centered. The display link requests up to 120 Hz only while settling; iOS controls the actual cadence.

Adaptive materials become opaque with Reduce Transparency. Increased Contrast reduces fading. Following [Apple's motion guidance](https://developer.apple.com/design/human-interface-guidelines/motion), Reduce Motion keeps the selection/connection information but disables depth, focus scaling, and the custom zoom transition. Large accessibility text sizes retain the complete thread list. VoiceOver exposes selection and a Highlight connections action. A system selection haptic fires only when focus changes to a thread, not on every drag update. Haptic strength is controlled by iOS/device settings.

The River's branch occupies a dedicated left gutter, separate from the media/text column. Full-row backgrounds draw the trunk at a fixed horizontal position, including row insets and tall media; zero row spacing and hidden separators let adjacent segments meet without gaps while retaining native List cell reuse. Event junctions remain near each header, with a small tributary at merge events. Loading earlier history uses the same rail. This is presentation-only and does not change event order, memberships, or navigation.

Junction dots and their short branches are attached to each title’s actual first text baseline, offset by half the Dynamic Type-scaled headline cap height for optical centering. They are independent of the full-height trunk, so wrapped titles, extra event-row padding, and taller media do not shift them away from the first line.

Threads now opens directly to the map; the former Timeline/Graph switch and timeline home have been removed. Find a thread provides a complete, local, name-searchable list with full Dynamic Type titles and memory counts. The map still renders at most forty circles, and its notice directs people to the uncapped finder. The old `remember.project.homeView` preference is retained but no longer read or rewritten.

Opening either a bubble or finder result goes directly to the named thread's history. Captures/imports and content revisions retain their original media; merges and accepted splits use compact markers. Routine operations are available through Activity & decisions, with an unresolved-suggestion count on the thread's entry point. Global activity is available from the Threads toolbar, and its source/date/thread filters apply only to activity. Historical activity uses the same dated snapshot as the history view. No ledger rows, memberships, grouping prompts or thresholds are changed. Known stock user-action wording is adapted for display only; stored rationales and generated/source evidence remain unchanged.

The River's continuous rail, media playback and direct Memory return remain. The vertical menu offers Rename thread and recoverable Archive thread. View past state retains the date slider and current-state comparison. Earlier streams appear under Merged threads. See [Threads interface and validation](threads-interface.md) for the current UI contract; dated validation entries below describe earlier versions.

Cloud assistance is off by default and affects subsequent capture enrichment, which can transmit source excerpts and images through the existing proxy. D3 thread matching does not call that proxy, even with the toggle enabled. Ask and AI search are independent explicit cloud actions; their on-demand embedding cache is separate from graph embeddings. Local capture/bootstrap does not call cloud embeddings. Apple model asset downloads do not send captured text. The old cloud organization/split code remains only in the legacy organizer. No billing or paid entitlement is added.

## Video imports

Implementation files (under `Remember/Remember/` unless noted):

- Added `PhotoVideoTransfer.swift`, `LocalVideoPlayerView.swift`, and `Remember/RememberTests/VideoRiverTests.swift`.
- Capture and storage types: `ContentView.swift`, `InAppCaptureService.swift`, `InAppCaptureViews.swift`, `LibraryViewModel.swift`, `MemoryItem.swift`, and `Remember/Shared/CaptureInbox.swift`.
- River and playback: `Provenance.swift`, `ProvenanceViews.swift`, `RiverMediaView.swift`, `AudioMemoryPlayerView.swift`, and `MemoryDetailView.swift`.
- Video labels/filtering/evidence: `AskRememberView.swift`, `LocalAI.swift`, `MemoryAnalyzer.swift`, `MemoryChunk.swift`, `MemoryContentExtractor.swift`, `OrganizeView.swift`, `ProjectGraphView.swift`, `ProjectIntelligence.swift`, and `ProjectView.swift`.
- Updated `Remember/RememberUITests/RememberUITests.swift`, this document, and `readme.md`. Existing dial, graph, project-file, and privacy-plist changes from earlier work are preserved.

The additive `video` raw value is supported by both the capture inbox and library; the database uses unrestricted text columns, so no destructive schema migration is required. Existing records retain their encoding. Do not downgrade to a build that cannot decode video records after importing videos.

Photos uses a [file-based transfer](https://developer.apple.com/documentation/coretransferable/filerepresentation) into a protected temporary file, validates playability/video tracks, then copies into the protected inbox/vault off the main actor. Cancel/failure removes only the newly created temporary copy. Saving/cancelling the preview releases its temporary file. Original Photos assets and existing vault files are never removed. Files imports retain security-scoped access until validation/copy finishes.

[Native AVKit playback](https://developer.apple.com/documentation/avkit/videoplayer) stays inline, does not autoplay on scrolling, and uses bounded poster-frame generation. Unsupported or unavailable originals show a recoverable error. Video capture enrichment now extracts speech and sampled-frame text/possible scene labels locally, with timestamped visual evidence. It remains partial because sampling cannot cover every scene. An additive coverage-note column and one-time upgrade of indexed caption-only videos are described in [video indexing](video-indexing.md). Video capture enrichment never invokes the cloud analyzer; explicit Ask/AI search retain their existing behavior.

## Validation

The River navigation/circular-map follow-up updates `ProvenanceViews.swift`, `RiverMediaView.swift`, `AudioMemoryPlayerView.swift`, `ProjectGraphView.swift`, `ProjectGraphLayout.swift`, `ProjectView.swift`, `ProjectViewModel.swift`, `ProjectGraphService.swift`, and `Provenance.swift`. Regression coverage lives in `ProvenanceTests.swift`, `GraphInteractionTests.swift`, `VideoRiverTests.swift`, and `RememberUITests.swift`; this document and `readme.md` describe the behavior. No dependencies or database migrations were added.

```sh
xcodebuild -project Remember/Remember.xcodeproj -scheme Remember \
  -sdk iphonesimulator -configuration Debug \
  -derivedDataPath /tmp/RememberProvenance CODE_SIGNING_ALLOWED=NO build

xcodebuild -project Remember/Remember.xcodeproj -scheme Remember \
  -destination 'platform=iOS Simulator,name=iPhone 17 Pro,OS=26.5' \
  -derivedDataPath /tmp/RememberProvenance CODE_SIGNING_ALLOWED=NO test
```

`ProvenanceTests` covers append-only protection, idempotent captures, retained archives, historical revision replay, stale extraction, merge/revert lineage, split acceptance, vector edge cases, pinned assignments, unavailable models, and a cloud-analyzer spy. The Project UI test captures a note and opens its River, scrubs history, and checks view persistence after relaunch. Existing tests continue covering capture, extraction, search, citation grounding, and manual collections.

Physical-device validation is needed for Apple model availability, asset download behavior, speech, and clustering quality. Cloud reasoning requires a configured proxy and is not exercised by deterministic tests.

`VideoRiverTests` covers video byte preservation/idempotent import, archive/restore, event-specific media revisions, no duplicate media on metadata events, caption-only/no-cloud analysis, independent temporary copies, and playable/corrupt/missing movie validation.

The video UI test imports a synthetic clip into Remember and must run only on a simulator. Seed the simulator Photos library first (replace the simulator ID):

```sh
video_fixture_dir=$(mktemp -d /tmp/remember-river-media.XXXXXX)
ffmpeg -hide_banner -loglevel error -f lavfi -i 'testsrc2=size=640x360:rate=24:duration=8' \
  -f lavfi -i 'sine=frequency=440:duration=8' -c:v libx264 -pix_fmt yuv420p -c:a aac \
  -shortest "$video_fixture_dir/river-fixture.mp4"
xcrun simctl addmedia <simulator-id> "$video_fixture_dir/river-fixture.mp4"
xcodebuild -project Remember/Remember.xcodeproj -scheme Remember \
  -destination 'platform=iOS Simulator,id=<simulator-id>' \
  -derivedDataPath /tmp/RememberProvenance -disableAutomaticPackageResolution CODE_SIGNING_ALLOWED=NO \
  -parallel-testing-enabled NO -only-testing:RememberTests \
  -only-testing:RememberUITests/RememberUITests/testPhotosVideoImportAndInlineRiverPlayback test
```

### Satin surfaces and interaction lighting — 10 September 2026

Added `ProjectGraphLighting.swift` with pure, bounded viewport-relative lighting and the shared node surface. Updated `ProjectGraphView.swift`, `GraphInteractionTests.swift`, `readme.md`, and this document. Removed the strong fixed white patch, doubled bright outline, and focus glow. A faint face reflection shifts by at most 6.5% horizontally and 4.5% vertically within the circle; a localized rim reflection turns at most 20 degrees from its resting direction. Both use the same presentation position as the grid, so they settle with the existing snap controller and remain unchanged at rest. No additional timer, sensor input, or idle animation is introduced. Tests cover bounded/continuous lighting, deterministic resting appearance, synchronized snap frames, and static Reduce Motion lighting.

The treatment uses standard content materials, following the distinction in [Apple's material guidance](https://developer.apple.com/design/human-interface-guidelines/materials); it does not apply Liquid Glass to every content node. Reduce Transparency uses an opaque base, Increased Contrast uses a stronger continuous outline, and Reduce Motion fixes the light direction. Labels, captions, fixed slots, snap selection/motion, navigation, and persisted data are unchanged.

Validation: `xcodebuild test` passed all 35 interaction tests plus the simulator hold/drag/open/return flow (`/tmp/remember-satin-nodes-tests.log`), then all 103 unit tests (`/tmp/remember-satin-nodes-all-unit.log`). Signed `xcodebuild build-for-testing` passed (`/tmp/remember-satin-nodes-device-build.log`), and `xcodebuild test-without-building` passed both safe physical iPhone checks: existing-library Memory/River navigation and map hold/drag/snap/return (`/tmp/remember-satin-nodes-phone.log`). Light simulator and dark phone screenshots were reviewed, including focused and snapped states. The signed app was installed in place with `xcrun devicectl`; personal memories and Git state were untouched. `git diff --check` passed. Existing simulator embedding-cache permission warnings and build warnings remain unrelated to this surface change. Automated trajectory checks do not certify sustained hardware frame rate or subjective lighting feel; device accessibility settings were not changed.

### Compact labels and centered full-title caption — 10 September 2026

Added `ProjectGraphLabel.swift`; updated `ProjectGraphLayout.swift`, `ProjectGraphView.swift`, `GraphInteractionTests.swift`, `RememberUITests.swift`, `readme.md`, and this document. Removed five-word/44-character truncation and per-line independent font shrinking. New coverage checks whole-word aliases, retained Dates/Assignments qualifiers, short/empty/Unicode titles, full-title accessibility, caption updates after settling/recentering, and unchanged canvas geometry across title lengths. Saved titles, thread organization, snap motion, and navigation are unchanged.

Validation: `xcodebuild test` passed 32 interaction tests plus the existing simulator flow (`/tmp/remember-map-labels-tests.log`), then all 100 unit tests and the caption-aware UI flow (`/tmp/remember-map-labels-verified.log`). After tightening the reserved caption to 44 scaled points, the simulator caption/snap/navigation flow passed again (`/tmp/remember-map-labels-caption-final.log`). Signed `xcodebuild build-for-testing` passed (`/tmp/remember-map-labels-device-final-build.log`). Both safe physical iPhone checks passed across runs: library/photo/River navigation (`/tmp/remember-map-labels-phone.log`) and the final caption/centering/preview flow (`/tmp/remember-map-labels-phone-final.log`). An earlier second phone check was interrupted by a foreground/tab change before the map was opened; it was rerun without changing production behavior or device settings.

Light simulator and dark phone screenshots were reviewed, including the fully visible “Digital Entrepreneurship Dates” label. The final app was installed in place and launched with `xcrun devicectl`. `git diff --check` passed; no personal memories, saved titles, dependencies, or Git state changed. The shortening is intentionally a first-pass heuristic: ambiguous aliases can still occur, with the full caption/River/VoiceOver title providing the exact identity. Exceptional unbroken text and accessibility-size caption scrolling remain useful hands-on review cases.

### Consistent snap animation — 9 September 2026 follow-up

The previous handoff combined automatic `GestureState` offset reset, committing the snap destination, and conditionally disabling implicit animation while dragging. Those independent updates could lose the release position or animate from the destination plus stale translation. Replaced that arrangement with `ProjectGraphMotion.swift`: one current offset and an interruptible, elapsed-time display-link settle. `ProjectGraphView.swift` now uses gesture state only to detect cancellation, not to calculate position or control animation transactions. The nearest-node selection and fixed honeycomb geometry are unchanged.

Changed this follow-up: `ProjectGraphMotion.swift`, `ProjectGraphView.swift`, `GraphInteractionTests.swift`, `RememberUITests.swift`, and this document. Unit coverage verifies the release position is preserved exactly, bounded monotonic steps at 30/60/120 Hz, exact completion, interruption from the visible position, stale-frame cancellation, no-op/reduced-motion settling, and cleanup. The safe UI flow also exercises repeated quick releases without an end hold.

Validation: `xcodebuild test` passed 31 interaction tests and the existing simulator map flow (`/tmp/remember-smooth-snap-tests.log`), then all 99 unit tests plus the expanded quick-release simulator flow (`/tmp/remember-smooth-snap-verified.log`). Signed `xcodebuild build-for-testing` passed (`/tmp/remember-smooth-snap-device-final-build.log`). The first phone checks sampled 3–4 points before settling completed: display-link motion is not covered by XCTest's implicit-animation idle wait. The harness now waits explicitly for both center coordinates within one point before final assertions, without relaxing their tolerances or changing the animation duration.

The final `xcodebuild test-without-building` run passed both safe physical iPhone checks, including repeated quick releases, reverse drags, exact centering, preview navigation, and direct Memory/River Back (`/tmp/remember-smooth-snap-phone-final.log`). The final phone map screenshot was inspected. The signed app was installed in place and launched with `xcrun devicectl`; personal memories and Git state were untouched. `git diff --check` passed. Deterministic frame tests cover the trajectory and interruption behavior, but do not certify sustained hardware frame rate or subjective haptic/animation feel; device accessibility settings were not changed.

### Snap-to-center browsing — 9 September 2026 follow-up

Added occupied-slot nearest-center snapping in `ProjectGraphLayout.swift` and wired release/accessibility settling in `ProjectGraphView.swift`. Live dragging stays unanimated and preserves rigid grid spacing; only settling animates. Updated `GraphInteractionTests.swift`, `RememberUITests.swift`, `readme.md`, and this document. Tests cover short-drag return, threshold crossing, reverse drags, exact centering, idempotent settling, outer-ring/empty/invalid inputs, and unchanged direct/preview River navigation. This supersedes the free-scrolling resting behavior in the previous fixed-grid implementation.

Validation: `xcodebuild test` passed 28 interaction tests and the simulator hold/snap/reverse/preview/River flow (`/tmp/remember-snap-grid-verified.log`), followed by all 96 unit tests (`/tmp/remember-snap-grid-all-unit.log`). Signed `xcodebuild build-for-testing` passed (`/tmp/remember-snap-grid-device-final-build.log`). The first compile required explicitly typing `CGFloat.nan` in an invalid-input test. An initial phone focus test began with an already-focused circle; its setup now uses Recenter before measuring idle size. Production behavior was not changed to satisfy that assertion.

Both device-safe physical iPhone checks passed via `xcodebuild test-without-building`: library/photo/River navigation (`/tmp/remember-snap-grid-phone.log`) and the corrected hold/snap/reverse/preview flow (`/tmp/remember-snap-grid-phone-final.log`). Light simulator and dark phone screenshots confirmed the neighboring thread lands at the same fixed center. The final signed app was installed in place and launched using `xcrun devicectl`. `git diff --check` passed; no personal captures or Git state changed. Hardware haptic feel, VoiceOver directional gestures, and subjective animation feel remain hands-on checks; no device accessibility settings were modified.

### Fixed honeycomb slots — 9 September 2026 follow-up

Replaced radial position distortion and focus-driven neighbor displacement with one rigid grid translation. Every circle's center remains its original hexagonal slot plus the shared pan offset, including during diagonal drags and focus changes. The existing radial size magnification, 6% focus lift, monochrome surfaces, hold preview, and direct River navigation remain. Grid pitch now reserves the maximum magnified/focused diameter plus a gap, so fixed slots do not produce overlaps. This supersedes the position-warping behavior described in the earlier September 9 implementation record below; there is no snapping or independent node rearrangement.

Changed in this follow-up: `ProjectGraphLayout.swift`, `ProjectGraphProjection.swift`, `GraphInteractionTests.swift`, `RememberUITests.swift`, `readme.md`, and this document. Regression coverage checks exact shared translation and separation for up to forty circles, different viewport sizes, multiple pan directions, and focus choices. The device-safe UI flow now compares neighboring centers before/after holding and dragging, in addition to magnification and navigation.

Validation: `xcodebuild test` passed 26 focused interaction tests and the simulator hold/drag/preview/River flow (`/tmp/remember-fixed-grid-tests.log`), followed by all 94 unit tests (`/tmp/remember-fixed-grid-unit-suite.log`). Signed `xcodebuild build-for-testing` passed (`/tmp/remember-fixed-grid-device-build.log`); `xcodebuild test-without-building` passed both safe physical iPhone UI checks (`/tmp/remember-fixed-grid-phone.log`). Light simulator and dark phone screenshots were inspected. The app was installed in place and launched with `xcrun devicectl`. `git diff --check` passed. No personal captures, persistence formats, dependencies, or Git state changed. Subjective animation/haptic feel remains a hands-on check.

### Honeycomb fish-eye and monochrome glass — 9 September 2026

Interaction research included [WatchLayout](https://github.com/HocTran/WatchLayout), whose public UIKit layout and SwiftUI wrapper expose distance-based circle scaling, and Apple's [materials guidance](https://developer.apple.com/design/human-interface-guidelines/materials). The implementation is original code within the existing SwiftUI map, not a copied/vendored package; no dependency, lockfile, or license changes were needed. The supplied monochrome reference guides the surface treatment, but icons/count badges and decorative orbit lines are intentionally not reproduced. Tap-to-River and drag-only navigation remain intact, with a preview available only on hold.

Changed files: `ProjectGraphLayout.swift`, `ProjectGraphProjection.swift`, `ProjectGraphView.swift`, `GraphInteractionTests.swift`, `RememberUITests.swift`, `readme.md`, and this document. New unit coverage verifies the six-neighbor hexagonal structure, largest-thread centering, radial position compression, monotonic drag direction, periphery-to-center growth, nonoverlap, projected edge endpoints, reachability of every circle, and Reduce Motion. UI checks now target the lens-center circle by its stable ID, verify shrinkage after panning away, and exercise the hold-preview route as well as direct taps. The first build caught a missing CoreGraphics import; the corrected geometry run passed 24 interaction tests and the existing focus/navigation UI check (`/tmp/remember-honeycomb-geometry.log`).

Final validation: `xcodebuild test` passed all 93 unit tests, including minimum touch-target coverage for fully visible lens circles, plus the focus/preview and accessibility-text UI checks (`/tmp/remember-honeycomb-verified.log`). The separate map drag/source-navigation simulator check also passed (`/tmp/remember-honeycomb-regression.log`). The accessibility fixture now creates a unique simulator-only title, avoiding an older source outside the loaded history window; no River production behavior was changed. Signed `xcodebuild build-for-testing` passed (`/tmp/remember-honeycomb-card-build.log`). Both safe physical iPhone checks passed across the final runs: library/photo/River navigation in `/tmp/remember-honeycomb-phone-verified.log`, and focus, lens magnification, repeated direct River visits, and preview-card navigation in `/tmp/remember-honeycomb-card-phone.log`. The latter also passed on the simulator (`/tmp/remember-honeycomb-card-simulator.log`).

An earlier phone run was interrupted when another app entered the foreground. A later preview-card tap failed; the card now has an explicit full rounded-rectangle touch target, and that route passed again on both devices. The idle map controls also retain their bottom-trailing alignment when the drawer is absent. Light/dark, held-focus, and large-text screenshots were reviewed. The final signed app was installed in place and launched using `xcrun devicectl`; personal captures and threads were not created, edited, or deleted. `git diff --check` passed and no Git state was changed. VoiceOver directional gestures, hardware haptic feel, and subjective motion smoothness remain hands-on checks; Reduce Motion geometry is unit-tested, without changing the user's device settings.

### Full-height, drag-only map — 8 September 2026

Moved the complete All threads list from Graph to Timeline, replacing the old horizontal topic chips. Graph now fills the remaining Project viewport with the map. Removed user magnification state, pinch gestures, zoom buttons, and fit-to-size behavior; retained focus/depth, direct River navigation, and a position-only Recenter control. Library growth no longer shrinks the circles. A containing accessibility group preserves individual node buttons and provides directional scroll actions; Timeline supplies full-size, wrapping titles at accessibility text sizes. The existing forty-circle map limit is unchanged and larger libraries are directed to Timeline.

Changed files for this follow-up: `ProjectGraphView.swift`, `ProjectGraphLayout.swift`, `ProjectView.swift`, `ProvenanceViews.swift`, `GraphInteractionTests.swift`, `RememberUITests.swift`, `readme.md`, and this document. No dependencies or persistence formats changed. The River change only reuses one historical snapshot within its event filter, avoiding repeated full replay per event without changing which events are included.

Validation: `xcodebuild test` passed all 90 unit tests and the map focus, drag/no-pinch-zoom/direct-River, and accessibility text checks (`/tmp/remember-map-browsing-final.log`). Additional simulator checks passed video importing/inline playback and thread edit/delete/restore/direct Memory Back (`/tmp/remember-map-browsing-timeline-final.log`). Signed `xcodebuild build-for-testing` passed (`/tmp/remember-map-browsing-device-final-build.log`); both safe physical iPhone checks passed via `xcodebuild test-without-building` (`/tmp/remember-map-browsing-phone-final.log`). These confirm the map occupies more than half the screen, zoom controls/list are absent, pinching does not magnify nodes, panning follows the drag, recentering restores position, holding does not navigate, and repeated River visits remain usable. Dark phone and light simulator map screenshots were reviewed. The app was installed in place and launched with `devicectl`; no test captures were created or edited on the phone.

Initial checks caught an accessibility container that hid its children; explicit containment fixed it. Test selectors were updated to retain node UUIDs across focus z-order changes, scroll the now-virtualized Timeline list to offscreen titles, and allow activity before a photo entry in the River. An older history check also missed the native switch using a fractional row coordinate; its harness now targets the actual switch and verifies its value. Once enabled, that check exposed a real history-mode stall: a process sample showed event filtering repeatedly replaying the entire historical snapshot inside its membership predicate. The filter now computes that snapshot once. The stalled simulator app was terminated after collecting the sample; the personal phone was not used for this diagnostic. Simulator embedding-cache warnings are unrelated to the map changes.

Final verification after the historical replay fix: `xcodebuild test` passed all 90 unit tests, `testMemoryMapAccessibilityTextSize`, and the complete `testProjectCaptureRiverHistoryAndViewPreference` flow (`/tmp/remember-map-browsing-history-fixed.log`). The latter now enables history, exposes the date slider, returns to Graph, preserves the selected view across relaunch, and reaches the same River through Timeline. Signed `build-for-testing` passed again (`/tmp/remember-map-browsing-history-device-build.log`), as did both safe physical iPhone UI checks (`/tmp/remember-map-browsing-phone-verified.log`). The final app was reinstalled in place and launched. `git diff --check` passed; no Git state was changed. VoiceOver directional actions and subjective haptic feel still need hands-on accessibility/device review; automated checks cover accessible node exposure and large-text Timeline navigation.

### Map focus and depth — 8 September 2026

Changed files: `ProjectGraphView.swift`, new `ProjectGraphProjection.swift`, `GraphInteractionTests.swift`, `RememberUITests.swift`, `readme.md`, and this document. No persistence, dependencies, River layout, or Git state changed. The new unit checks cover viewport depth, focus lift and neighbor displacement, projected edge endpoints/hit testing, nonoverlap at multiple zoom levels/library sizes, and the Reduce Motion geometry path.

`xcodebuild test` passed all 88 unit tests and three simulator UI checks: `testExistingMapFocusHoldDragAndReturn`, `testMemoryMapSelectionZoomAndSourceNavigation`, and `testMemoryMapAccessibilityTextSize` (`/tmp/remember-map-focus-regression.log`). The focus check confirms holding does not navigate, dragging does not activate the node, clearing/fit restores the overview, and two successive River visits return to a usable circle. Light/large-text screenshots were exported for visual review. Signed `xcodebuild build-for-testing` passed (`/tmp/remember-map-focus-device-verification-build.log`).

The initial compile exposed a non-Hashable navigation payload; routing now uses the stable thread UUID. The first device attempt timed out while enabling iOS automation before app tests ran. Reinstalling the signed runner allowed tests to start. Subsequent device navigation checks were interrupted by system notification banners covering Back; the test harness now waits for those banners to disappear, without opening or dismissing their contents. Hardware haptic feel and subjective motion smoothness still require hands-on judgment; automated tests do not certify either.

Final verification: both physical iPhone checks passed (`testExistingLibraryGraphNavigationWithoutCaptures` and `testExistingMapFocusHoldDragAndReturn`, `/tmp/remember-map-focus-phone-final.log`). The corrected focus harness also passed again on the simulator (`/tmp/remember-map-focus-final-simulator.log`). Light/dark held-focus and large-text screenshots were inspected. The final signed app was installed in place and launched; no test captures were added to the personal phone and no personal memories/threads were edited or archived. `git diff --check` passed. Simulator embedding-asset permission warnings remain unrelated to this display-only change; no new claim about live embedding quality is made.

### Verified implementation run — 7 September 2026

First-line dot alignment follow-up changed `ProvenanceViews.swift`, the accessibility River screenshot check in `RememberUITests.swift`, and this document. `xcodebuild test` passed `testMemoryMapAccessibilityTextSize`; signed `build-for-testing` and the physical iPhone `testExistingLibraryGraphNavigationWithoutCaptures` via `test-without-building` passed. Screenshots were inspected at normal and accessibility XXXL text sizes, including wrapped and single-line titles; dots align with the first line and the trunk remains continuous. `git diff --check` passed. Logs: `/tmp/remember-river-dot-alignment-simulator.log`, `/tmp/remember-river-dot-alignment-device-build.log`, `/tmp/remember-river-dot-alignment-phone.log`. This presentation-only change did not require a unit-suite rerun or alter personal captures.

Continuous-branch follow-up changed `ProvenanceViews.swift`, `RememberUITests.swift`, `readme.md`, and this document. `xcodebuild test` passed 20 graph/media unit tests and both simulator UI regressions (video importing/inline playback and thread edit/delete/restore/direct Back). The signed `xcodebuild build-for-testing` passed; the connected iPhone's `testExistingLibraryGraphNavigationWithoutCaptures` passed via `test-without-building`, including matching header/photo alignment. Exported phone screenshots confirmed the trunk stays continuous beside a tall photo and across the boundary to the next entry. The app was installed in place and launched, and `git diff --check` passed. Logs: `/tmp/remember-continuous-river-tests.log`, `/tmp/remember-continuous-river-device-build.log`, and `/tmp/remember-continuous-river-phone.log`. No personal captures were added or deleted.

River controls/circular-map follow-up: `xcodebuild test` passed 83 unit tests and the simulator video-import/inline-playback regression. A separate corrected `testThreadMenuEditDeleteRestoreAndDirectMemoryBack` run passed, including title editing, deletion without removing the memory, archive restoration, and direct Memory → River navigation. The signed `xcodebuild build-for-testing` passed with `DEVELOPMENT_TEAM=397P48LWC5`; `test-without-building` on the connected iPhone passed `testExistingLibraryGraphNavigationWithoutCaptures`, including circle geometry, pan/zoom/fit, and photo → Memory → River navigation. Final physical map/River screenshots were inspected, and the app was launched in place. No test captures or thread deletions were performed on the personal phone.

Relevant logs: `/tmp/remember-circle-river-regression.log` (83 unit tests and video passed; the initial menu text-edit selector failed), `/tmp/remember-circle-river-menu-final.log` (15 graph/interaction tests and corrected menu regression passed), `/tmp/remember-circle-river-device-verified-build.log`, and `/tmp/remember-circle-river-phone-verified.log`. Earlier checks exposed an unnecessary map accessibility wrapper, a photo selector that expected an image instead of the new button, and a rename-test caret position issue; these were corrected and rerun. A device build initially omitted the test targets’ team setting and passed after supplying the existing team on the command line, without editing project signing settings.

The final simulator rerun (`/tmp/remember-circle-river-final-validation.log`) passed all 83 unit tests again, including bounded whole-word circle titles, plus `testMemoryMapAccessibilityTextSize`. `git diff --check` passed. Voice-label navigation uses the same direct route as photo/header navigation; voice hardware recording and playback were not separately rerun in this follow-up. The simulator's Apple embedding-cache permission warnings remain an environment limitation, not a certification of live model behavior.

Video/River follow-up: `xcodebuild test` passed 79 unit tests and the capture/history/archive UI regression on the isolated iPhone 17 Pro simulator. The final separate `testPhotosVideoImportAndInlineRiverPlayback` run passed (36.7 seconds), including native playback position advancing. Earlier UI-test selectors were corrected for Photos thumbnails and AVKit's duplicate slider labels; testing also found and fixed premature temporary-file cleanup during preview handoff and an oversized River marker.

The signed `xcodebuild build-for-testing` passed. Three non-mutating physical iPhone UI checks passed (detail add-button visibility, dial fling/dismiss, graph-to-River navigation), followed by a successful graph/River layout rerun and screenshot inspection. A transient test-runner trust error was resolved by reinstalling the same signed runner. The final app was installed in place and launched; no synthetic captures were added to the personal phone.

Final run logs: `/tmp/remember-video-final-regression.log` (79 unit tests and capture/history UI passed; the then-ambiguous video slider selector failed), `/tmp/remember-video-playback-final.log` (corrected end-to-end video test passed), `/tmp/remember-video-final-device-build.log`, `/tmp/remember-video-device-final.log`, and `/tmp/remember-video-phone-layout-rerun.log`. `git diff --check` passed. iCloud-only downloads, very large clips, and HEVC/HDR playback on physical hardware remain manual checks; no automatic video transcription or scene analysis is implemented.

The simulator build passed. The final regression command below passed **49 unit tests and 3 UI tests (52 total, zero failures)** on iPhone 17 Pro / iOS 26.5. `git diff --check` passed. Exported graph and historical River screenshots were visually inspected.

```sh
xcodebuild -project Remember/Remember.xcodeproj -scheme Remember \
  -destination 'platform=iOS Simulator,id=EC7418D3-29BD-4E1A-BBAD-24037D5420F9' \
  -configuration Debug -derivedDataPath /tmp/RememberProvenance \
  CODE_SIGNING_ALLOWED=NO -parallel-testing-enabled NO \
  -only-testing:RememberTests \
  -only-testing:RememberUITests/RememberUITests/testProjectCaptureRiverHistoryAndViewPreference \
  -only-testing:RememberUITests/RememberUITests/testRadialCaptureMenuExposesEveryCaptureAction \
  -only-testing:RememberUITests/RememberUITests/testThreeSurfaceNavigationAndTemporaryAI test
```

The simulator reported a permission failure loading Apple’s embedding cache under `/var/db/com.apple.naturallanguaged`. Named singleton fallback, capture, historical browsing, and correction flows continued to work. Real embedding quality, Foundation Models generation, and live cloud reasoning have not been certified by this run. No Git index, refs, history, or repository configuration were changed.

### Physical-device follow-up — 7 September 2026

The separate iPhone 17 probe successfully loaded the real Apple model and checked the actual graph service against synthetic data in temporary databases. Runtime, vector validity, repeatability, and graph idempotence passed; clustering quality failed. In one arrival order all twelve notes from four distinct topics joined one cluster. See the [device evaluation and recorded metrics](evaluations/2026-09-07-iphone17-embeddings.md). The installed Remember app and real library were left untouched, and production behavior was not changed in this diagnostic pass.

### Clustering fix and expanded validation — 7 September 2026

The source-grounded dual-embedding policy subsequently passed nine device arrival-order scenarios across 42 synthetic notes, with no mixed-topic clusters and correct four-topic grouping of the original twelve notes. One unavailable embedding retained a singleton. The expanded unit suite passed 61 tests, and all three UI regressions passed. See the [fix, results, compatibility behavior, and limitations](evaluations/2026-09-07-grounded-clustering.md).

The final clean-harness rerun of the cached implementation also passed all 90 checks after the phone was unlocked, confirming the same nine-scenario results. Its [separate report](evaluations/2026-09-07-grounded-clustering-final-rerun.json) is retained alongside the original evaluation.
